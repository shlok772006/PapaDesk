rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow public read and write access for dev testing
      allow read, write: if true;
    }
  }
}